<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="@dimen/standardMargin6x"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_margin="@dimen/standardMargin"
    android:background="@drawable/search_linear_shape"
    android:gravity="center_vertical"
    android:layoutDirection="rtl"
    android:orientation="horizontal">

    <ImageView
        android:id="@+id/search_image"
        app:tint="@color/search_text_one"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layoutDirection="rtl"
        android:src="@drawable/ic_baseline_search_24" />

    <LinearLayout
        android:id="@+id/search_text_linear"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:orientation="horizontal">

        <com.dust.exmall.customviews.CTextView
            android:id="@+id/search_text"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="center_vertical"
            android:layout_marginStart="@dimen/standardMargin"
            android:text="جستوجو در"
            android:textColor="@color/search_text_one"
            android:textSize="@dimen/TextSize4x" />

        <TextView
            android:id="@+id/exMallText"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="center_vertical"
            android:layout_marginStart="@dimen/standardMargin0.5x"
            android:text="ExMall"
            android:textColor="@color/search_text_two"
            android:textSize="@dimen/TextSize5x"
            android:textStyle="bold|italic" />
    </LinearLayout>

</LinearLayout>
